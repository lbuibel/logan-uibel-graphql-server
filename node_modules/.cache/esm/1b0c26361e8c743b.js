let PrismaClient;_1e8‍.w("@prisma/client",[["PrismaClient",["PrismaClient"],function(v){PrismaClient=v}]]);

const  prismaClient = new PrismaClient()

async function createRoute() {
    try {
        await prismaClient.route.create({
            data: {
                name: 'Test Post',
                type: 'Paved',
                miles: 10.0,
                startPoint: 'starting point',
                endPoint: 'end poing',
                startingElevation: 10,
                finalElevation: 200,
            }
        })
    } catch(err) {
    _1e8‍.g.console.log(err)
    }
}

async function main() {
    try {
        await createRoute()
    }
    catch(err){
        _1e8‍.g.console.log(err)
    }
}

main()
.catch(e => _1e8‍.g.console.error(e))
.finally(async () => {
    await prismaClient.disconnect()
})